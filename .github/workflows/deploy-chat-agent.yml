# name: Deploy Chat Agent
# on:
#   push:
#     paths:
#       - 'chat-agent/**'
#   workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'yarn'
#           cache-dependency-path: 'chat-agent/yarn.lock'

#       - name: Install dependencies
#         working-directory: ./chat-agent
#         run: yarn install --frozen-lockfile

#       - name: Run tests (si tienes)
#         working-directory: ./chat-agent
#         run: yarn test || echo "No tests configured"

#       - name: Deploy to Cloudflare Workers
#         working-directory: ./chat-agent
#         env:
#           CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#           CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
#         run: |
#           # Configura según tu despliegue en Cloudflare Workers
#           yarn deploy || echo "Ajusta este comando según tu configuración"
