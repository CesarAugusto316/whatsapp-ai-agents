# name: Deploy CMS API
# on:
#   push:
#     paths:
#       - 'cms-api/**'
#   workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Bun
#         uses: oven-sh/setup-bun@v1
#         with:
#           bun-version: latest

#       - name: Install dependencies
#         working-directory: ./cms-api
#         run: bun install

#       - name: Build (si es necesario)
#         working-directory: ./cms-api
#         run: bun run build || echo "No build script"

#       - name: Run tests
#         working-directory: ./cms-api
#         run: bun test || echo "No tests configured"

#       - name: Build Docker image
#         working-directory: ./cms-api
#         run: |
#           docker build -t cms-api:latest .
#           # O usa docker-compose si prefieres
#           # docker-compose build

#       - name: Deploy (ajusta según tu infra)
#         run: |
#           echo "Configura aquí tu despliegue a Dokploy/Docker"
#           # Ejemplo básico:
#           # docker save cms-api:latest | ssh user@server "docker load"
